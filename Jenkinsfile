@Library("jenkins-shared-library")

    stages {
        stage('build') {
            steps {
					mvn --version
            }
        }
		stage('Deploy') {
            steps {
					echo "This is it"
            }
        }
    }
