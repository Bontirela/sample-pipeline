@Library("jenkins-shared-library")

pipeline {
    stages {
        stage('build') {
            steps {
					mvn --version
            }
        }
		stage('Deploy') {
            steps {
					echo "This is it"
            }
        }
    }
}