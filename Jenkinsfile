@Library("jenkins-shared-library")

node('master') {
   
        stage('build') {
            steps {
					mvn --version
            }
        }
		stage('Deploy') {
            steps {
					echo "This is it"
            }
        }
    
}
